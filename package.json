"scripts": {
  "start": "npx vite preview --host 0.0.0.0 --port $PORT",
  "build": "vite build",
  "dev": "vite"
},
"dependencies": {
  "@anthropic-ai/sdk": "^0.20.0"
},
"devDependencies": {
  "vite": "^5.0.0",
  "@vitejs/plugin-react": "^4.0.0"
}
